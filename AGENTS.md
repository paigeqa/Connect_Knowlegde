## Imported Claude Cowork project instructions

Cloud Connect(CoC) QA 작업 공간. 담당 QA는 Paige.

## 일하는 방식 (먼저 읽는다)

### 쉬운 말로 쓴다

어려운 말을 쓰지 않는다. 추상적으로 압축한 표현을 쓰면 읽는 사람이 한 번 더 해석해야 한다. 결론이 맞아도 전달이 안 된다.

- 한 문장에 개념 하나
- 비유보다 구체적인 예시
- 전문용어를 쓸 거면 바로 옆에 풀어 쓴다
- 문단보다 목록·표를 먼저 고려한다
- 동사를 자연스럽게 — "절차를 10단계로 박았습니다"가 아니라 "10단계로 정리했습니다". 멋있게 쓰려다 이상해지는 쪽보다 평범하고 정확한 쪽이 낫다

### 시작 신호가 있을 때만 실행한다

파일을 만들거나 고치는 작업은 사용자가 "시작하자"고 할 때만 한다. 그 전에는 **같이 전략을 짜는 단계**다.

- 방향을 묻는 질문("어떻게 관리하니?", "이렇게 하면 어때?")에는 조사·읽기·분석까지만 하고 답변으로 끝낸다
- 판단이 필요한 지점은 혼자 정하지 말고 선택지를 제시해 같이 결정한다
- 되돌리기 어려운 일(케이스 폐기, 티켓 등록, 외부에 남는 글)은 반드시 먼저 확인받는다

### 틀렸으면 짧게 고치고 넘어간다

이 레포 작업 중 실제로 틀린 적이 있다. 길게 사과하지 않고 무엇이 틀렸는지만 말하고 고친다.

## 스펙의 기준은 SSOT다

**https://ssot.protopie.works/ko/connect/**

제품이 어떻게 동작하는지는 SSOT를 본다. 코드 근거(파일:줄)와 변경 이력이 붙어 있는 살아 있는 문서다.
이 레포는 SSOT를 대체하지 않는다. **SSOT가 다루지 않는 것만** 담는다.

## 이 레포로 하는 일 4가지

1. SSOT + `spec.md`를 기준으로 체크리스트 작성
2. 체크리스트를 TestRail에 편입
3. TestRail을 최신 스펙 기준으로 업데이트
4. CoC 스펙을 이해하고 버그 티켓 작성

## 폴더 지도

| 경로 | 역할 | 고쳐도 되나 |
|---|---|---|
| `spec.md` | SSOT에 없는 스펙. 권한 모델, 미결 질문, 레거시 차이, 에러 목록, REST API | O — 스펙이 확정되면 갱신 |
| `qa-guide.md` | 테스트 기준. 환경 매트릭스, 위험 우선순위, 제외 범위, 옛 케이스 변환 | O |
| `sources/` | 원본 문서 아카이브. 스쿼드 종료(2026-06-30) 시점 Notion 최종본 + 레거시 공식 문서 + 강의자막 | X — 동결. 인용만 |
| `testrail/` | 체크리스트 → TestRail 변환·업로드 스크립트, 런 관리, 옛 케이스 CSV | O |
| `archive/` | 옛 문서 보관 (`Cloud_Connect_Spec_rev16.md` = 분리 전 원본 150KB) | X — 보관용 |
| `build/` | 스크립트 산출물. git 미추적, 재생성 가능 | 신경 안 써도 됨 |
| `.claude/skills/` | 스킬 4개 (아래 표) | O |

체크리스트 자체는 **레포에 없다.** 릴리즈 문서별로 Notion에 있고, 작업할 때 링크를 받는다.

## 스킬

에이전트는 없다. **스킬 하나 = 절차 하나**로 관리한다. 예전에 에이전트 5개짜리 파이프라인을 만들었다가 안 써서 지웠다(커밋 `06f09fc`). 같은 구조를 다시 만들지 않는다.

| 스킬 | 하는 일 | 산출물 |
|---|---|---|
| `ssot-sync` | SSOT ↔ `spec.md`·`qa-guide.md` 대조. 우리 문서를 최신으로 맞춘다 | `ssot-diff.md` (반영 후 삭제) |
| `checklist-review` | SSOT + `spec.md` ↔ 스프린트 체크리스트 대조. 프로세스 1단계 | 대화만. 파일 없음 |
| `testrail-migrate` | Notion 체크리스트 메뉴 → TestRail suite 업로드 | TestRail 케이스 |
| `concise-docs` | 문서를 결론 우선·중복 없는 형태로 다듬는다 | 문서 수정 |

두 대조 스킬을 헷갈리지 않는다. `ssot-sync`는 **우리 문서**를 고치고, `checklist-review`는 **체크리스트**를 고친다. `ssot-sync`가 먼저다 — 우리 문서가 낡으면 체크리스트를 낡은 기준으로 대조하게 된다.

아직 스킬이 없는 것:

| 없는 스킬 | 4가지 일 중 | 만들려면 필요한 것 |
|---|---|---|
| 체크리스트 초안 작성 | 1번 | 초안을 처음부터 쓰는 경우의 기준 |
| TestRail 최신화 | 3번 | TestRail 케이스를 읽어 비교하는 방식 |
| 버그 티켓 작성 | 4번 | Jira `PS` 프로젝트. 티켓 예시 1~2개 |
| 회귀 런 관리 | — | `testrail/runs/` 스크립트 2개를 절차로 감싸기 |

## 레포 루트의 md는 3개뿐이다

`spec.md` · `qa-guide.md` · `AGENTS.md`

`ssot-diff.md`는 `ssot-sync` 작업 중에만 존재하는 네 번째 파일이다. 반영 후 삭제하고 커밋하지 않는다(`.gitignore`). 그 외 md를 루트에 만들지 않는다 — 파일이 쌓이면 어느 게 최신인지 몰라 두 군데서 관리하게 되고, 그게 이 레포를 정리한 이유였다.

대조 이력은 파일이 아니라 두 곳에 남는다.

| 무엇 | 어디 |
|---|---|
| 언제 어디까지 대조했나 | `spec.md`·`qa-guide.md` frontmatter의 `ssot_reviewed` |
| 무엇을 어떻게 바꿨나 | git 커밋 메시지 |

## 지킬 것

- **SSOT와 겹치는 내용을 `spec.md`에 쓰지 않는다.** 겹치면 SSOT가 정답. 두 군데서 관리하면 반드시 어긋난다.
- `sources/`는 원본 아카이브다. 고치지 않는다. 갱신이 필요하면 Notion MCP로 다시 받아 덮어쓴다.
- API 키는 `testrail/.env`에서만 읽는다. 출력·로그·커밋에 노출 금지.
- TestRail 업로드는 항상 `--dry-run` 먼저.
- 이미지 S3 링크는 Notion fetch 후 1시간 만료. 추출을 지체하지 않는다.

## 지금 무엇을 제공하고 있나

**Enterprise만 제공한다.** 기업이 서버를 사고, 그 안에 CoC를 추가 구매하는 형태다.

- **등급(tier)·플랜 매트릭스 테스트를 하지 않는다.** 등급 게이팅이 2026-08-13에 코드에서 제거됐다
- 주 게이트는 Edit Role ≥ Editor. 단 플랜 존재 게이트(Stage 생성 등)는 별도로 있다 → `spec.md` §0, Q-18
- Self-serve는 정식 출시 정책 확정 후 — 2026년 10월 결정 예정

## 알려진 상태 (2026-08-30)

- SSOT 1단계 대조를 마쳤다 (changelog 2026-08-19 ~ 08-20, 61개 항목 — changelog HTML 직접 파싱으로 전수 확인). 결과는 `spec.md`·`qa-guide.md`에 반영됨.
- 이번 대조의 큰 것 3개: ① **Map Navigation 레이어 신설** (Engine 레이어 4종이 됨, qa-guide에 전용 절) ② **플랜 존재 게이트 발견** (spec §0 정정, Q-18) ③ **격리·권한 경계 정정** — 격리는 Team 소속 기준, rename·삭제는 서버가 팀 소속만 검증 (F-IDM-team-root·F-IDM-server-role-gate).
- ⚠️ **공백 구간**: SSOT changelog는 2026-07-27부터 시작한다. 2026-06-12 ~ 07-27 사이 6주의 변경은 changelog에 없다. 이 구간 변경은 SSOT 페이지 본문을 직접 봐야 잡힌다.
- `spec.md` §4에 미결 질문 14건이 있다(2026-08-30에 Q-17·Q-18·Q-19 추가, 음성은 Q-13에서 해결). 우선순위 높은 것은 Q-18(플랜 게이트 실효성), Q-17(IFTTT 상충), Q-16(REST API 잔존 여부).
- S280(309 케이스)은 등급 게이팅 제거로 회귀 시드에서 제외했다. 원본 CSV는 남아 있다.
- ⚠️ changelog fetch는 WebFetch 요약을 믿지 말 것 — 항목 누락·번호 밀림이 실제로 났다. `curl`로 HTML을 받아 직접 파싱하는 게 정확하다.
- `sources/Notion/SSOT.md`는 SSOT가 웹사이트로 옮겨가기 전(2026-07-02) Notion 스냅샷이다. 최신이 아니다. 웹 SSOT를 본다.

## QA 프로세스 (스쿼드 규칙)

체크리스트를 만들다가 차이점이나 질문이 생기면 **단계를 올려가며** 확인한다. 앞 단계에서 풀리면 다음으로 안 간다.

1. **1단계** — 최신 SSOT ↔ 테스트 초안 비교 (에이전트 활용 등 빠른 방법 아무거나)
2. **2단계** — 개발자 코드 ↔ 테스트 초안 비교
3. **3단계** — 개발자에게 직접 테스트 초안 리뷰 요청

질문은 `spec.md` §4에 기록하고, 확정되면 해당 항목을 `미결` → `확정`으로 바꾼다.

## 다음에 할 일 (2026-08-17 기준)

대화 기록은 기기마다 따로 저장되므로 넘어오지 않는다. 이어서 할 때 여기서 시작한다.

### 1. 버그 티켓 스킬 만들기 — 4번

Sprint #4에서 탐색적으로 찾은 버그들이 대상이다. **양식을 먼저 알아야 한다.** 둘 중 하나가 필요하다.

- Atlassian MCP 연결 — OAuth 엔드포인트 `https://mcp.atlassian.com/v1/mcp/authv2` (Cloud 전용, Jira read·write·search).
  커넥터 디렉터리: https://claude.ai/directory/connectors/atlassian
  또는 터미널에서 `claude mcp add --transport http atlassian https://mcp.atlassian.com/v1/mcp/authv2`
- 아니면 기존 티켓 2~3개를 붙여주기 — 양식만 알면 되는 거라면 이게 더 빠르다

Jira 프로젝트 키는 `PS` (`protopie.atlassian.net/browse/PS-####`). 실제 티켓 예: PS-1068, PS-1070, PS-1084~1088, PS-1173, PS-1179, PS-1180.

TestRail 결과에 `defects` 필드가 이미 있고 `testrail/runs/build_r2.py`가 회귀 런 이월 시 그대로 옮긴다. 티켓 키가 들어가는 자리다.

**스킬을 만들 때 물어볼 것**: 필수 필드(Severity·Priority·Component·Label·Affects Version), 제목 규칙, 재현 단계 형식, 첨부 사용법, TestRail `defects` 연동 여부.

양식 채우기에서 그치지 않고 `spec.md`·`qa-guide.md`를 근거로 **"이게 진짜 버그인지"** 판단하게 만든다. 기대결과가 SSOT와 다르면 버그가 아니라 스펙 질문일 수 있다.

### 2. 6주 공백 메우기

2026-06-12 ~ 07-27 구간은 SSOT changelog에 없다. `ssot-sync` 4단계(페이지 본문 직접 확인)로 메운다. PIN 폐기 같은 큰 변경이 이 구간에서 온 것으로 보인다.

### 3. 미결 질문 12건

`spec.md` §4. 대부분 2단계(개발자 코드 확인) 대상이다.

## 새 기기에서 시작할 때

레포에 없는 것은 다시 만들어야 한다.

| 필요한 것 | 방법 |
|---|---|
| `testrail/.env` | `TESTRAIL_USER` / `TESTRAIL_KEY` 새로 작성. 커밋 금지 |
| `build/` 산출물 | 없어도 된다. 스크립트가 재생성한다 |
| 이전 대화 기록 | 넘어오지 않는다. 이 문서와 git 커밋 메시지가 대신한다 |
| Notion 권한 | `.claude/settings.json`에 허용 규칙이 있다. 단 커넥터 ID(`mcp__<UUID>__notion-fetch`)가 기기마다 다르면 안 맞는다. 그때는 한 번 더 허용하고 그 ID로 갱신한다 |
